{% extends 'base.html.twig' %}

{% block title %}GenreProduit index{% endblock %}

{% block body %}
    <h1>GenreProduit index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Genre</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for genre_produit in genre_produits %}
            <tr>
                <td>{{ genre_produit.id }}</td>
                <td>{{ genre_produit.genre }}</td>
                <td>
                    <a href="{{ path('app_genre_produit_show', {'id': genre_produit.id}) }}">show</a>
                    <a href="{{ path('app_genre_produit_edit', {'id': genre_produit.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_genre_produit_new') }}">Create new</a>
{% endblock %}
