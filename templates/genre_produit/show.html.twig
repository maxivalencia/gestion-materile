{% extends 'base.html.twig' %}

{% block title %}GenreProduit{% endblock %}

{% block body %}
    <h1>GenreProduit</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ genre_produit.id }}</td>
            </tr>
            <tr>
                <th>Genre</th>
                <td>{{ genre_produit.genre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_genre_produit_index') }}">back to list</a>

    <a href="{{ path('app_genre_produit_edit', {'id': genre_produit.id}) }}">edit</a>

    {{ include('genre_produit/_delete_form.html.twig') }}
{% endblock %}
