<form method="post" action="{{ path('app_mouvement_delete', {'id': mouvement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ mouvement.id) }}">
    <button class="btn">Delete</button>
</form>
