{% extends 'base.html.twig' %}

{% block title %}Unité{% endblock %}

{% block body %}
    <h1>Unité</h1>

    <table class="table">
        <tbody>
            <!-- <tr>
                <th>Id</th>
                <td>{{ unite.id }}</td>
            </tr> -->
            <tr>
                <th>Nom</th>
                <td>{{ unite.nom }}</td>
            </tr>
            <tr>
                <th>Sigle</th>
                <td>{{ unite.sigle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_unite_index') }}" class="btn btn-primary kl-bouton"><i class="fa fa-arrow-left" aria-hidden="true"></i> Retour</a>
    <br>
    <a href="{{ path('app_unite_edit', {'id': unite.id}) }}" class="btn btn-success kl-bouton"><i class="fa fa-pencil-square-o" aria-hidden="true"></i> Modifier</a>
    <br>
    {{ include('unite/_delete_form.html.twig') }}
{% endblock %}
