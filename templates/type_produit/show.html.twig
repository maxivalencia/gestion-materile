{% extends 'base.html.twig' %}

{% block title %}Type de Produit{% endblock %}

{% block body %}
    <h1>Type de Produit</h1>

    <table class="table">
        <tbody>
            <!-- <tr>
                <th>Id</th>
                <td>{{ type_produit.id }}</td>
            </tr> -->
            <tr>
                <th>Type</th>
                <td>{{ type_produit.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_type_produit_index') }}" class="btn btn-primary kl-bouton"><i class="fa fa-arrow-left" aria-hidden="true"></i> Retour</a></a>
    <br>
    <a href="{{ path('app_type_produit_edit', {'id': type_produit.id}) }}" class="btn btn-success kl-bouton"><i class="fa fa-pencil-square-o" aria-hidden="true"></i> Modifier</a>
    <br>
    {{ include('type_produit/_delete_form.html.twig') }}
{% endblock %}
