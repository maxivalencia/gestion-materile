{% extends 'base.html.twig' %}

{% block title %}TypeMouvement{% endblock %}

{% block body %}
    <h1>TypeMouvement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_mouvement.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ type_mouvement.type }}</td>
            </tr>
            <tr>
                <th>Sens</th>
                <td>{{ type_mouvement.sens }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_type_mouvement_index') }}">back to list</a>

    <a href="{{ path('app_type_mouvement_edit', {'id': type_mouvement.id}) }}">edit</a>

    {{ include('type_mouvement/_delete_form.html.twig') }}
{% endblock %}
