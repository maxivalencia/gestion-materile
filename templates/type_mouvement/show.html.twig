{% extends 'base.html.twig' %}

{% block title %}Type de Mouvement{% endblock %}

{% block body %}
    <h1>Type de Mouvement</h1>

    <table class="table">
        <tbody>
            <!-- <tr>
                <th>Id</th>
                <td>{{ type_mouvement.id }}</td>
            </tr> -->
            <tr>
                <th>Type</th>
                <td>{{ type_mouvement.type }}</td>
            </tr>
            <tr>
                <th>Sens</th>
                <td>{{ type_mouvement.sens }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_type_mouvement_index') }}" class="btn btn-primary kl-bouton"><i class="fa fa-arrow-left" aria-hidden="true"></i> Retour</a>
    <br>
    <a href="{{ path('app_type_mouvement_edit', {'id': type_mouvement.id}) }}" class="btn btn-success kl-bouton"><i class="fa fa-pencil-square-o" aria-hidden="true"></i> Modifier</a>
    <br>
    {{ include('type_mouvement/_delete_form.html.twig') }}
{% endblock %}
